# 周一任务 · 详细指导（四周通用）

> 配套文件：`Python学习计划_网页版.html`（打卡表）、`Python学习计划_四周.xlsx`（存档）
> 环境：Windows 10/11 · 编辑器 VS Code · 语言 Python 3.x

四周里的每个周一都是"工作日 30 分钟"档，**目标不是学得多，是学完能立刻看到结果**。
下文第 1 周周一给出逐步操作清单（含每一步的验证方法），第 2–4 周周一给出可直接照抄的代码骨架和验收清单。

---

## 第 1 周周一 · 环境搭建（安装 Python 和 VS Code；运行 Hello World）

**完成标志：能成功打印 Hello World**

### 0. 先做现实判断（2 分钟）

这台电脑上原来的 Python 已经打不开了（`D:\新建文件夹\python.exe` 已不存在，`python` 命令会跳到应用商店）。
所以今天的任务对你是**真的从零安装**，不是走形式，按下面顺序来。

### 1. 装 Python（约 10 分钟）

1. 打开 <https://www.python.org/downloads/windows/> 下载**最新的 Windows installer (64-bit)**（当前稳定版是 3.14 系列，装哪个 3.x 都行，不用纠结）。
2. 双击安装包，**第一屏底部必须勾上 `Add python.exe to PATH`** —— 这是新手第一个大坑，忘了勾后面命令找不到。
3. 点 `Install Now`（默认路径 `C:\Users\<你>\AppData\Local\Programs\Python\...` 就行，**路径别带中文和空格**），等它装完点 `Close`。

**验证：** 按 `Win + R` 输入 `cmd` 回车，在黑色窗口里敲：

```bat
python --version
```

看到 `Python 3.x.x` 就成功。如果弹出应用商店或提示"不是内部或外部命令"，说明 PATH 没勾上 —— 重跑安装包，选 `Modify` 把 PATH 补上，或者直接勾 `Add to PATH` 重装一遍。

再敲一条确认 pip 也在（以后装库要用）：

```bat
pip --version
```

### 2. 装 VS Code 并加 Python 扩展（约 8 分钟）

1. 打开 <https://code.visualstudio.com/> 下载安装，一路默认下一步（可以勾上"添加到 PATH"和"资源管理器右键菜单"）。
2. 打开 VS Code，点左侧栏的**扩展**图标（四个方块那个，快捷键 `Ctrl+Shift+X`）。
3. 搜索 `Python`，安装微软官方那个（作者是 **Microsoft**，下载量最高的那个）。它会自动带上 Pylance。
4. 中文界面可选：搜 `Chinese (Simplified)` 装一下，重启 VS Code 就是中文菜单。

### 3. 建工作文件夹 + 写第一个程序（约 7 分钟）

在 VS Code 里：`文件 → 打开文件夹`，**新建并打开**一个专门放代码的文件夹，比如：

```
D:\Python学习\
  week1\
```

（别把代码散落在桌面；后面每周一个子文件夹，最后直接传 GitHub，省事。）

然后在左侧文件列表空白处右键 → `新建文件`，命名 `hello.py`。**扩展名必须是 `.py`**，写成 `hello.py.txt` 是不会运行的。

输入下面三行（**建议手敲，不要复制**，敲一遍才知道分号、引号、括号在哪）：

```python
print("Hello World")
print("我是 ZERO，今天开始学 Python")
print("2026 年，我要学会写程序")
```

按 `Ctrl + S` 保存。**VS Code 标签页上的小圆点消失才代表存了 —— 没保存的运行结果永远是旧的。**

### 4. 运行（约 3 分钟）

三种方式任选，**推荐第二种**：

| 方式 | 操作 | 适合 |
|---|---|---|
| 终端命令 | 在 VS Code 里按 `` Ctrl+` `` 打开终端，敲 `python hello.py` | 最通用，和以后工作一样 |
| 运行按钮 | 右上角三角形 ▶（先点它右边的下拉，选 `Run Python File`） | 最省事，推荐新手 |
| 交互式 | 终端敲 `python` 进入 `>>>`，逐行敲 `print("hi")`，`exit()` 退出 | 试语法、算数，不写文件 |

**验证：** 终端里出现

```
Hello World
我是 ZERO，今天开始学 Python
2026 年，我要学会写程序
```

出现这三行 = **今天的完成标志达成**，可以去网页版把 D01 点亮成“已完成”。

### 5. 顺手把三个高频报错先认识一下（约 2 分钟）

| 现象 | 原因 | 怎么修 |
|---|---|---|
| `'python' 不是内部或外部命令` | 装 Python 时没勾 PATH | 重跑安装包选 Modify 补 PATH |
| `can't open file 'hello.py'` | 终端当前目录不是文件所在目录 | 先 `cd /d D:\Python学习\week1`，或直接用 ▶ 按钮运行 |
| `SyntaxError: invalid syntax` | 少了括号/引号，或用了中文标点 | 检查 `()` `""` 是不是英文半角；`print` 后必须有括号 |

### 6. 今日小挑战（选做，1 分钟）

把第一行改成 `print("Hello World")` 之外的内容，比如打印你的名字和今天的日期，再运行一次。
**能自己改出结果，比看懂教程强十倍。**

### ✅ D01 打卡清单

- [ ] `python --version` 能输出版本号
- [ ] VS Code 装了 Microsoft 的 Python 扩展
- [ ] 有一个自己的代码文件夹（如 `D:\Python学习\week1`）
- [ ] `hello.py` 能跑出 3 行输出
- [ ] 至少故意制造过 1 个报错，并知道它为什么报错

---

## 第 2 周周一 · if 基本语法（写判断是否成年）

**完成标志：能写出一个 if 判断**

### 核心语法（今天只需要记住这一件事）

```python
age = 20                     # 1. 先有一个值
if age >= 18:                # 2. 冒号不能少
    print("已成年")           # 3. 缩进 4 个空格，代表"属于 if"
print("程序结束")             # 4. 不缩进 = 无论如何都执行
```

- `if` 那行结尾的 **`:` 是必须的**；
- 下面那行必须**缩进**（VS Code 里按一次 `Tab`，或 4 个空格）—— Python 用缩进划分代码块，这是它和别的语言最大的不同；
- `>=` 是“大于等于”，`==` 才是“等于”（`=` 是赋值，别混）。

### 照抄起步，然后自己改

```python
age = int(input("请输入你的年龄："))

if age >= 18:
    print("你已成年")

print("判断结束")
```

跑通之后依次做这三个改动，每个改动都重新运行：

1. 把 `18` 改成 `22`，输入 `20` 试试 —— 观察“不打印”也是一种结果；
2. 把 `age = int(input(...))` 里的 `int` 去掉，输入 `18` 再试 —— 体会为什么需要 `int()`；
3. 把 `print("判断结束")` 前面的空格删掉、再加回来 —— 看缩进变化带来的报错或行为差异。

### 常见坑

- `if age >= 18` 后面**忘了冒号** → `SyntaxError: expected ':'`
- 缩进用空格和 Tab 混着 → `IndentationError`（统一用 4 个空格最稳）
- 输入的数字直接比较报 `TypeError` → `input()` 拿到的是**字符串**，比大小前要 `int()` 转成整数

### ✅ 打卡清单

- [ ] 能默写出 `if 条件:` + 缩进块的结构
- [ ] 程序能根据输入的不同，分别打印或不打印
- [ ] 知道 `=` 和 `==` 的区别

---

## 第 3 周周一 · while 循环（写 1 到 10 累加）

**完成标志：能写出 while 循环**

### 核心语法

```python
i = 1            # 1. 起点
total = 0        #    累加器
while i <= 10:   # 2. 条件：成立就再跑一圈
    total += i   # 3. 循环体（缩进）
    i += 1       # 4. 让条件迟早不成立 —— 忘了这行就是死循环！
print(total)     # 55
```

**while 的三个零件：起点、条件、变化量。缺了变化量，程序会永远转下去**（终端卡住时按 `Ctrl + C` 强制停下）。

### 照抄起步，然后自己改

先跑这个“看得见过程”的版本，比直接算总和更容易理解：

```python
i = 1
while i <= 10:
    print("现在是第", i, "次循环")
    i += 1
print("循环结束")
```

再看累加版（就是上面那段），并回答自己两个问题：

1. 把 `i += 1` 删掉会发生什么？（**先猜，再试**，然后用 `Ctrl + C` 停下）
2. 把 `while i <= 10` 改成 `while i < 10`，结果从 55 变成多少？为什么？

### 常见坑

- 忘了 `i += 1` → 死循环（这是新人排名第一的错误，遇到不是你的电脑坏了）
- `total += i` 写成 `total = i` → 循环能跑完，但结果是最后一个数而不是总和
- 缩进多了一层/少了一层 → 结果只算一次或报错
- 循环体里 `print` 缩进错了 → 你会发现数字只打印一次

### ✅ 打卡清单

- [ ] 能写出“起点 / 条件 / 变化量”三件套
- [ ] 1 到 10 累加输出 55
- [ ] 亲眼见过一次死循环，并会用 `Ctrl + C` 停

---

## 第 4 周周一 · 复习变量与数据类型（5 道小题）

**完成标志：能独立完成小题**

这一天的重点不是学新东西，是**关掉教程自己写**。下面 5 题可以直接当练习，每题限时 5 分钟左右，写不出来再看提示。

```python
# 1. 定义 5 个不同类型的变量（int / float / str / bool / list），用 print 全部打印出来

# 2. 输入两个数字，输出它们的和、差、积、商（保留 2 位小数，用 f-string）

# 3. 输入姓名和年龄，输出："我叫小明，今年 20 岁，明年 21 岁"

# 4. 用 type() 打印上面每个变量的类型，看看和你以为的一不一样

# 5. 输入一个字符串，输出它的长度、第一个字符、最后一个字符
```

**参考答案要点**（先自己写，卡住再看）：

```python
# 1
num = 10
price = 9.9
name = "ZERO"
is_student = True
scores = [90, 85, 77]
print(num, price, name, is_student, scores)

# 3
name = input("姓名：")
age = int(input("年龄："))
print(f"我叫{name}，今年{age}岁，明年{age + 1}岁")

# 5
s = input("输入一段文字：")
print(len(s), s[0], s[-1])
```

### 高频错点

- `"20" + 1` → 报 `TypeError`：字符串和数字不能直接相加，要 `int("20") + 1`，或统一用 f-string 拼接
- f-string 忘记写 `f`：`print("{age} 岁")` 会原样输出大括号
- `s[0]` 和 `s[-1]` 弄反：`-1` 是**最后一个**字符
- `len()` 只对字符串、列表这类"有长度"的东西有效，对数字用会报错

### ✅ 打卡清单

- [ ] 5 道题全部独立写出并运行成功
- [ ] 能说清 `int` / `float` / `str` / `bool` 各自长什么样
- [ ] 会用 f-string 拼字符串

---

## 通用建议（四周都适用）

1. **30 分钟怎么分**：10 分钟看语法 → 15 分钟手敲并改着玩 → 5 分钟写一句"今天学会了什么"。
2. **手敲 > 复制**：复制粘贴跑通的是程序，手敲跑通的才是你的。
3. **报错先读最后一行**：Python 的报错信息最后一行点名了错误类型，倒数第二三行有文件名和行号，**先看行号再去那一行找问题**。
4. **卡超过 20 分钟就记下来**：写进网页版的备注框，周日复盘再统一查，别让一次卡住毁掉整周节奏。
5. **每周日必须收尾**：把 `week1`、`week2` 文件夹传到 GitHub，笔记一起传。四周后你手上就是一个能给人看的作品集。

---

## 2026-09-12 环境备注

写这份指导时检查过这台电脑：

- `python` 命令指向 Microsoft Store 占位程序，实际 Python 未安装；
- `py` 启动器记录的 `D:\新建文件夹\python.exe` 已不存在；
- 未检测到 `pip`、`openpyxl`。

所以第 1 周周一的安装步骤是**必做**项，不是可跳过项。
