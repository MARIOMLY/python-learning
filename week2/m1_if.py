# m1_if.py —— 第 2 周周一 · if 基本语法：判断是否成年
# 运行方式： python m1_if.py

age = int(input("请输入你的年龄："))

if age >= 18:
    print("你已成年")

print("判断结束")

# ---------- 跑通之后，依次做这三个改动，每次改完重新运行 ----------
# 改动 1：把 18 改成 22，输入 20，观察"什么都不打印"也是一种结果
# 改动 2：把 int() 去掉，输入 18，看看报什么错、为什么
# 改动 3：把最后一行 print 前面的缩进加到 if 里面，比较两次运行的区别
